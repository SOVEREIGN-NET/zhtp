//! Shared identity functionality
//! 
//! DID management and identity wallets used by all node types.

// Identity functionality is already in lib-identity
// Import directly from lib_identity in your code instead of using this re-export
